/**
 * Create two Loggers, that represent two ways of logging: logging into console and logging into file.
 * There are no tests. So, made yours own tests for each logger.
 * */

interface Logger {
    fun makeLog(message: String)
}