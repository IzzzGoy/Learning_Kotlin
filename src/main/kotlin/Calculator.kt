/**
 * Create a simple calculator, that consume expressions like 1 + 2 or 5 * 1.
 * Expression must contain two numbers and an operation.
 * Calculator must handle exceptions like: Divide by zero,  more, then one operation and wrong quantity of numbers in expression.
 * @param expression that calculator must solve
 * @return result of calculation
 * */

fun calculate(expression: String): Double {
    return 0.0
}
